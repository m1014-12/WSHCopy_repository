import { en } from './en';
import { ar } from './ar';

// Export all translations
export const translations = { en, ar };

// Export individual language objects for direct access
export { en, ar };

// Export available languages
export const availableLanguages = ['en', 'ar'];

// Export language names for display
export const languageNames = {
  en: 'English',
  ar: 'العربية'
};

// Default language
export const defaultLanguage = 'en';
